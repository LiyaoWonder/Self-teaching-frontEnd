#include<math.h>
int mathProblem()
{
    int x;
    x = -1;
    abs(x);
    double y;
    y = -0.002;
    fabs(y);
    // double z;
    // sin(z);
    // all data is double tan(z); eX exp(x); xY pow(x, y); aqure root sqrt(x);
    // ln --> log(x)  log10 --> log10(x)
    return 0;
};
